{
  "name": "meetmind-ai",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "extension"
  ],
  "scripts": {
    "dev": "npm run dev -w frontend",
    "build": "npm run build -w frontend && npm run build -w extension",
    "typecheck": "npm run typecheck -w frontend && npm run typecheck -w extension",
    "test:ui": "node scripts/ui-smoke.mjs"
  },
  "devDependencies": {
    "playwright": "^1.51.0"
  }
}
